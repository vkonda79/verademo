name: Veracode Scan Pipeline

on:
  push:
    branches: [main]
  pull_request:

jobs:
  veracode-scan:
    runs-on: ubuntu-latest
    steps:

      # 6️⃣ Generate PDF report using Veracode Java API
      - name: Generate PDF report
        run: |
          java -jar tools/VeracodeJavaAPI.jar \
            -vid "${{ secrets.VERACODE_API_ID }}" \
            -vkey "${{ secrets.VERACODE_API_KEY }}" \
            -action getreport \
            -appid "${{ secrets.VERACODE_API_ID_VAL }}" \
            -sandboxname "${{ secrets.SANDBOX_NAME }}" \
            -format pdf \
            -outputfilepath veracode_scan_report.pdf

      # 7️⃣ Upload PDF artifact
      - name: Upload PDF report
        uses: actions/upload-artifact@v4
        with:
          name: veracode_pdf_report
          path: veracode_scan_report.pdf
